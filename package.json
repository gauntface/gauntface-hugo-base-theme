{
  "scripts": {
    "build": "rm -rf build/ && tsc && npm run copy && node rollup.js",
    "copy": "cp -r src/layouts/ build/layouts/ && cp -r src/assets/ build/assets"
  },
  "devDependencies": {
    "glob": "^7.1.7",
    "rollup": "^2.51.2",
    "rollup-plugin-terser": "^7.0.2"
  }
}
