{
  "scripts": {
    "build": "rm -rf build/ && mkdir -p build/ && npm run copy && tsc && node rollup.js",
    "copy": "cp -r src/layouts/ build/layouts/ && cp -r src/assets/ build/assets && cp README.md build/README.md && cp theme.toml build/theme.toml",
    "build-example": "npm run build && rm -rf test/example-site/themes/base-theme/ && rm -rf test/example-site/public && cp -r build test/example-site/themes/base-theme && cd test/example-site && hugo --environment development",
    "test": "npm run build-example && ava ./test/*-test.js --verbose --timeout=2m"
  },
  "devDependencies": {
    "ava": "3.15.0",
    "glob": "7.1.7",
    "postcss": "8.3.11",
    "postcss-cli": "9.1.0",
    "postcss-css-variables": "0.18.0",
    "puppeteer": "14.3.0",
    "rollup": "2.51.2",
    "rollup-plugin-terser": "7.0.2",
    "static-server": "2.2.1",
    "typescript": "4.4.4"
  }
}
