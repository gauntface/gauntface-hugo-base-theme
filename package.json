{
  "scripts": {
    "build": "rm -rf build/ && mkdir -p build/ && npm run copy && tsc && node rollup.js",
    "copy": "cp -r src/layouts/ build/layouts/ && cp -r src/assets/ build/assets && cp README.md build/README.md && cp theme.toml build/theme.toml",
    "build-example-dev": "npm run build && rm -rf test/example-site/themes/base-theme/ && rm -rf test/example-site/public && cp -r build test/example-site/themes/base-theme && cd test/example-site && hugo --environment development",
    "build-example-prod": "npm run build && rm -rf test/example-site/themes/base-theme/ && rm -rf test/example-site/public && cp -r build test/example-site/themes/base-theme && cd test/example-site && hugo",
    "test-dev": "npm run build-example-dev && ava ./test/dev-*-test.js --verbose --timeout=2m",
    "test-prod": "npm run build-example-prod && ava ./test/prod-*-test.js --verbose --timeout=2m",
    "test": "npm run test-dev && npm run test-prod"
  },
  "devDependencies": {
    "ava": "4.3.0",
    "glob": "^8.0.3",
    "postcss": "8.4.14",
    "postcss-cli": "9.1.0",
    "postcss-css-variables": "0.18.0",
    "puppeteer": "14.3.0",
    "rollup": "2.75.6",
    "rollup-plugin-terser": "7.0.2",
    "static-server": "2.2.1",
    "typescript": "4.7.3"
  }
}
